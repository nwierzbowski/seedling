# Static Assets - src/assets/

This directory contains staticassets used by the frontend application.

## 📝 File Scope

This `CLAUDE.md` contains only details pertinent to the `src/assets/` level. For component styling, see `src/components/`.

## Assets

- **react.svg**: React logo SVG used as a placeholder

## Patterns

- SVG and image assets for UI elements
- Version-controlled static resources
