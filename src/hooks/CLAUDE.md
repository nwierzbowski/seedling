# Custom Hooks - src/hooks/

This directory contains custom React hooks for reusable stateful logic.

## 📝 File Scope

This `CLAUDE.md` contains only details pertinent to the `src/hooks/` level. For component-related logic, see `src/components/`.

## Hooks

- **useShiftAHotkey.ts**: Hook for listening to Shift+A keyboard shortcut

## Patterns

- Composition with existing React hooks
- Proper cleanup on unmount
- Type-safe event handlers
